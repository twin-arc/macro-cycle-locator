---
name: macro-cycle-locator
description: 判断当前宏观经济周期处于哪个阶段（衰退放水/复苏扩张/过热通胀/滞胀加息），并在给定个股时定位该股所属板块与产业链位置在当前阶段处于什么位置、该配还是该回避。基于六维打分卡（增长、通胀、中国货币、外部流动性、信用传导、市场定价）+ ETF 资金流高频校准 + 三层传导链（宏观定位→板块适配→个股验证）。当用户询问"现在是什么阶段""当前时点如何判断""板块轮动到哪了""某某股票处在什么位置""这个板块现在能不能配""周期定位""四季时钟"时使用本技能。适用于 A 股与港股市场的择时与配置判断。
agent_created: true
---

# 宏观周期定位与个股板块定位

## 用途

回答两类问题：

1. **现在处在周期的哪个阶段？** —— 输出一个阶段读数（阶段1 / 1.5 / 2 / 3 / 4）。
2. **某只个股所在的板块，现在处在什么位置？** —— 输出板块适配档位（该配 / 慎配 / 回避 / 左侧机会）
   以及该个股必须验证的指标。

底层框架是**分子端（经济增长与企业盈利）与分母端（货币与流动性）双变量驱动的四季轮动**：

| 阶段 | 增长 | 通胀 | 货币 | 主导资产 |
|---|---|---|---|---|
| 阶段1 衰退·放水 | 下行 | 低位 | 宽松 | 债券、科技成长 |
| 阶段2 复苏·扩张 | 上行 | 低位 | 仍宽 | 消费、科技（业绩兑现） |
| 阶段3 过热·通胀 | 强 | 上行 | 开始收 | 周期股（量价齐升） |
| 阶段4 滞胀·加息 | 下行 | 高 | 收紧 | 必选消费、高股息、医药 |

## 何时使用

- 用户问「现在是什么阶段」「当前时点怎么判断」「周期定位」「板块轮动到哪了」
- 用户提供股票名称或代码，问「这只股现在处在什么位置」「这个板块还能不能配」
- 用户要做资产配置、调仓，或判断某次上涨/下跌的性质

## 工作流

### 第一部分：判断当前时点

1. 读取 `references/scorecard.md`，**逐项跑完六个维度**并拉取最新实际数据。
   六个维度：增长动能 / 通胀结构 / 中国货币流动性 / 外部流动性 / 信用传导 / 市场定价。
2. **执行噪音剔除**——这一步不能省。上游商品大涨、PPI 高企时极易误判为阶段3，
   必须先检验是否为供给型通胀（判据：需求端未同步扩张、中游未同步盈利、涨价未放量、
   涨价源于供给事件）。详见 scorecard.md 第三节。
3. **校验切换确认信号**（scorecard.md 第四节）。未达成则只能标注「未确认」或「X.5 阶段」，
   **不得升级阶段**。最硬的一条是 M1−M2 剪刀差转正。
4. 读取 `references/etf-calibrator.md`，用 ETF 资金流校准第六维。
   **打分卡与 ETF 结论冲突时以 ETF 为准**。核心是量价背离四象限。
5. 输出阶段读数 + 六维读数表 + 未达成的确认信号 + 下一个决断窗口（关键事件与日期）。

### 第二部分：定位个股所在板块

用户给出个股时，读取 `references/sector-map.md`，按四步法执行：

1. **归类**到六大板块之一（周期上游 / 中游加工 / 科技成长 / 可选消费 / 必选消费 / 医药 / 金融红利）
2. **定位产业链位置**——第一判据：这家公司是**卖资源的，还是买资源的**？
   同一个「上游涨价」宏观事实，在两者身上的结论完全相反。
3. **查阶段适配矩阵**，得出该配 / 慎配 / 回避 / 左侧机会
4. **周期类个股额外套用反直觉铁律**：高 PE 买、低 PE 卖

然后读取 `references/stock-checklist.md` 做第三层个股验证，输出必验指标、
三个关键跟踪信号与证伪条件。

**必须在输出中显式声明能力边界**：打分卡是配置层工具，只能可靠用到第二层（板块适配）。
第三层（个股验证）打分卡已失效，必须用财报与产业链数据独立验证。

### 第三部分：自检与输出

1. 读取 `references/pitfalls.md`，逐条对照十个已知陷阱与输出前自检清单。
2. 生成报告：复制 `assets/report_template.html` 到工作目录并填充。
   该模板为浅色主题单文件 HTML、移动端适配、表格化呈现。
3. 用 present_files 展示结果。

## 输出要求

- **数据必须实时拉取**，不得凭记忆或先验给出读数。每项关键数据标注是「已发布值」还是
  「机构预测值」，并指出下一个官方发布时点。
- **主动标注数据缺口**，不要为了结论完整而填补不存在的数据。
- **冲突要保留而不是抹平**：六个维度指向不一致时，显式说明冲突并解释如何取舍，
  不要强行平均成一个阶段。
- **必须给出证伪条件**：什么数据出现说明当前判断错误。
- **主动修正历史结论**：若新数据推翻此前判断，明确写出「对上一版结论的修正」及原因。
- 用户偏好：专业简洁的单文件 HTML（支持手机浏览）、表格化呈现、指标对比、
  状态标签化；结论先行。

## 资源索引

| 文件 | 内容 |
|---|---|
| `references/scorecard.md` | 四季定义、六维打分卡、噪音剔除规则、阶段切换确认信号、失效场景 |
| `references/sector-map.md` | 三层传导链、六大板块阶段适配矩阵、产业链位置判据、渗透率曲线 |
| `references/stock-checklist.md` | 第三层个股体检清单（按资产类型：周期/科技/消费/医药/金融红利） |
| `references/etf-calibrator.md` | 量价背离四象限、资金结构形状、三个数据坑、校准执行清单 |
| `references/pitfalls.md` | 十个已知陷阱与输出前自检清单 |
| `assets/report_template.html` | 单文件 HTML 报告模板（浅色、移动端适配、表格化） |

读取顺序：先 `scorecard.md` 定位阶段 → 再 `etf-calibrator.md` 校准 →
涉及个股时读 `sector-map.md` 与 `stock-checklist.md` → 输出前读 `pitfalls.md` 自检。
